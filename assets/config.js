// Public configuration only. Never put passwords, API secrets or session tokens here.
// Empty = same-origin API when served by the Node backend.
// GitHub Pages: set this to your deployed HTTPS backend origin, without /api.
window.AMT_CONFIG = Object.freeze({ apiBase: '' });
