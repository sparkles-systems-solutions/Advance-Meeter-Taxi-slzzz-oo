PORT=5055
AMT_DATABASE=./data/taxi.sqlite
# Exact frontend origin; never use a wildcard. For local use:
AMT_ORIGIN=http://localhost:5055
# For GitHub Pages set AMT_ORIGIN=https://sparkles-systems-solutions.github.io
# Set production when HTTPS is provided by a reverse proxy on the same machine.
NODE_ENV=development
